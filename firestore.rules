rules_version = '2';

// Prázdny základ - zatiaľ nikto nesmie nič čítať ani zapisovať.
// Pravidlá doplň, keď bude jasné, aké dáta appka ukladá.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
